# 시후 하루 계획표 (Study Plan Circle)

24시간 원형 생활계획표 위에 **계획(Plan)** 과 **실행(Actual)** 을 나란히 기록하고,
그 차이를 분석해서 다음 계획을 개선하는 도구. **가족이 함께 보는 온라인 웹 앱**
하나로만 운영한다 (로컬 데스크톱판은 폐기했다 — [10. 연혁](#10-연혁) 참고).

---

## 1. 목적

- 하루 계획을 원 그래프(24시간 시계형)로 직관적으로 입력한다.
- 같은 계획표를 오른쪽에 복사해 두고, 그 위에 **실제 실행**을 겹쳐서 기록한다.
- 계획 대비 실행의 차이(활동별 시간, 시간대별 일치율)를 수치와 그래프로 보여준다.
- 이 데이터를 근거로 "계획을 어떻게 수정할지" 가이드를 얻는다.
- 시후와 부모님이 **각자 기기에서 같은 링크**로 열어 같은 기록을 본다.

---

## 2. 화면 구성

```
┌───────────────────────────────────────────────────────────────────────────┐
│  시후의 하루 계획표   [◀] 2026-08-16 (일) [▶]   [저장][되돌리기][주간][공유]│
├──────────────────────────────┬────────────────────────────────────────────┤
│         📘 계획 (Plan)        │          ✅ 실행 (Actual)                  │
│                              │                                            │
│         ┌──────────┐         │          ┌──────────┐                      │
│         │  24시간   │         │          │ 계획(흐림) │  ← 계획이 배경으로   │
│         │  원그래프  │         │          │ +실행(진함)│     그대로 복사됨   │
│         │ 이름 라벨  │         │          │ 이름 라벨  │  ← 블록 위에 이름   │
│         └──────────┘         │          └──────────┘     표시, 클릭해 편집 │
│                              │                                            │
│  [활동 팔레트]                │  [활동 팔레트]                              │
│  ● 수학 ● 영어 ● 국어 ...     │  ● 수학 ● 영어 ● 국어 ...                   │
│                              │  [계획 그대로 복사 ⧉]                       │
│  시간 블록 리스트 (편집/삭제)   │  시간 블록 리스트 (편집/삭제)                │
├──────────────────────────────┴────────────────────────────────────────────┤
│  📊 계획 vs 실행 분석                                                       │
│  일치율 78%  |  활동별 계획/실제 막대 비교  |  자동 코멘트(개선 가이드)        │
└───────────────────────────────────────────────────────────────────────────┘
```

- 왼쪽/오른쪽 원은 **같은 크기, 같은 좌표계**. 오른쪽 원에는 계획이 항상
  옅은 톤(투명도 42%)으로 전체에 깔려 있고, 실행 기록이 그 위에 **안쪽 진한
  띠**(전체 반지름의 3/5)로 겹쳐진다.
- 화면이 좁으면(<1100px) 위/아래로 자동 스택.

---

## 3. 원 그래프 규격

| 항목 | 값 |
|---|---|
| 형태 | 24시간 시계형 원 (0시 = 12시 방향, 시계방향) |
| 1시간 | 15° |
| 최소 입력 단위 | 10분 (스냅) |
| 계획 표현 | 꽉 찬 부채꼴(파이) |
| 실행 표현 | 안쪽 진한 띠, 눈에 보이는 면적이 전체 원의 3/5이 되도록 반지름을 √(3/5)배로 계산(단순 반지름 3/5배는 면적이 36%로 작아 보임). 계획은 전체에 옅게 깔려 바깥쪽에서 비쳐 보임 |
| 이름 라벨 | 안쪽 원과 바깥 원 사이 정중앙에 표시. 50분 이상은 가로로, 22~50분은 그 블록의 반지름 방향(안쪽 원→바깥 원)을 "세로"로 보고 한 글자씩 쌓아서(원 위쪽 절반은 순서 반전, 항상 화면 위→아래로 올바르게 읽힘), 22분 미만은 점선을 원 바깥 여백까지 끌어내 그 끝에 이름을 적는 "콜아웃"으로 표시한다. 인라인 글자색은 배경색 밝기에 따라 흰/검정 자동 선택, 콜아웃은 점선을 활동 색으로 그려 어느 블록인지 잇는다 |
| 눈금 | 매시 눈금, 3시간마다 숫자(0,3,6,9,12,15,18,21) |
| 렌더링 | SVG (선명하고 클릭/드래그 좌표 계산이 정확함) |

주간 요약의 요일별 미니 원그래프에는 이름 라벨을 넣지 않는다(너무 작아 안 보임).

---

## 4. 입력 방식

1. **원 위 드래그** — 활동 팔레트에서 활동 하나를 선택한 뒤, 원 위에서 시작 각도부터
   끝 각도까지 드래그하면 그 구간이 그 활동 색으로 칠해진다. 드래그 중에는
   커서 옆에 `07:30 → 09:00 (1시간 30분)` 툴팁이 실시간 표시된다.
2. **블록 편집** — 오른쪽 리스트에서 시작/종료 시간을 select 박스로 직접 수정, 삭제.
3. **겹침 처리** — 새 블록이 기존 블록과 겹치면 **새 블록이 이깁니다**(기존 블록이
   잘리거나 분할됨). 되돌리기(⌘Z/Ctrl+Z) 지원.
4. **지우개** — 팔레트의 지우개를 선택하고 드래그하면 그 구간이 비워진다.
5. **실행 쪽 전용** — `계획 그대로 복사 ⧉` 버튼 한 번으로 계획을 실행 쪽에 통째로
   복사한 뒤, 달라진 부분만 고쳐 그리면 된다(가장 빠른 기록 방법).
6. **지금 기록** — 실행 쪽에 "지금까지 ○○ 함" 버튼: 마지막 기록 시각 ~ 현재 시각을
   선택한 활동으로 채운다.
7. **이름·색 편집** — 원 위에 표시된 활동 이름 글자를 클릭하면 편집창이 뜨고,
   이름과 색을 바로 고칠 수 있다. 저장하면 그 활동을 쓰는 모든 블록(과거 기록
   포함)에 새 이름·색이 한꺼번에 반영된다. 이름 글자를 클릭하는 것이므로,
   글자가 없는 짧은 블록(50분 미만)은 팔레트에서 눌러 고를 순 있어도 원 위
   클릭으로는 편집할 수 없다 — 그 활동이 들어간 다른 긴 블록이나, 이 활동으로
   새로 그려서 늘린 뒤 편집하면 된다.
8. **활동 관리** — 팔레트의 `⚙ 관리` 버튼을 누르면 활동 전체 목록이 뜨고,
   블록을 먼저 그리지 않아도 이름·색을 바꾸거나 새로 추가하거나(하단 입력폼)
   `×` 로 삭제할 수 있다. 삭제해도 이미 그려둔 블록은 안 지워지지만, 그 활동의
   id(예: `play`)가 그대로 이름처럼 보인다 — 삭제 전 확인창에서 미리 알려준다.
   "활동 하나를 두 개로 나누기"는 기존 활동 이름을 하나로 바꾸고(예: `국어/독서`
   → `국어`) 새 활동을 하나 추가하는(`독서`) 방식으로 한다 — 과거에 그려둔
   블록은 나눌 수 없으므로 이 방식이 기록을 안 잃는 유일한 방법이다.

---

## 5. 활동(카테고리)

기본 제공 10종 — 이름/색 모두 원 위 라벨을 클릭해 언제든 바꿀 수 있다.
Firestore의 `families/{familyId}/meta/activities` 문서에 저장된다.

| 활동 | 색 | 그룹 |
|---|---|---|
| 수학 | `#4C6FFF` | 공부 |
| 영어 | `#8B5CF6` | 공부 |
| 국어 | `#14B8A6` | 공부 |
| 생물 | `#22A559` | 공부 |
| 물리 | `#0EA5E9` | 공부 |
| 화학 | `#D6409F` | 공부 |
| 식사 | `#F2B134` | 생활 |
| 취미 | `#F2994A` | 생활 |
| 휴식 | `#9AA5B1` | 생활 |
| 이동 | `#C7CDD6` | 생활 |

`＋ 활동` 으로 새 활동을 추가하면 기본 그룹은 `공부`다. 원 가운데 "공부 시간"
합계는 `group: "공부"` 인 활동들만 더한다 — 생활 활동으로 두고 싶으면 편집창
(라벨 클릭)에서 이름/색만 바꿀 수 있고, 그룹은 아직 UI로 못 바꾼다(필요하면
Firestore 콘솔에서 `meta/activities` 문서의 해당 항목 `group` 값을 직접 고친다).

---

## 6. 분석 (하단 패널)

- **시간 일치율(%)** — 10분 슬롯 144칸 기준, 계획 활동 == 실행 활동인 슬롯 비율.
- **공부 시간 총합** — 계획 vs 실제 (예: `계획 5h 20m → 실제 4h 10m, -1h 10m`).
- **활동별 비교 막대** — 활동마다 계획/실제 두 줄 막대와 증감(±분).
- **가장 많이 어긋난 시간대 Top 3** — 예: `16:00–17:00 계획 "수학" → 실제 "취미"`.
- **자동 코멘트(가이드)** — 규칙 기반 문장. 예시:
  - 계획 대비 실제가 계속 짧은 공부 활동 → "물리는 3일 연속 계획보다 20분 이상
    짧습니다. 한 블록을 40분으로 줄이고 쉬는 시간을 사이에 넣어보세요."
  - 특정 시간대 이탈 반복 → "16시대는 3일 연속 계획대로 되지 않았습니다."
  - 계획에 없던 활동이 반복 등장 → "계획에 없던 취미가 3일 나타났습니다."
- 메모칸 1개(그날 한 줄 회고).

---

## 7. 주간 요약 대시보드

헤더의 `주간 요약`(또는 `⌘W`/`Ctrl+W`)으로 열리는 전체 화면 대시보드.
주는 **월요일 시작**이며 `‹ ›` 또는 `←`/`→` 로 주 단위 이동한다.

- **KPI 4종** — 주간 공부 시간(계획 대비 증감), 평균 일치율, 기록한 날 수,
  가장 잘 지킨 요일
- **요일별 미니 원그래프 7개** — 안쪽 실행 / 바깥 계획(라벨 없음). 오늘은 테두리
  강조, 클릭하면 그 날짜의 편집 화면으로 이동한다.
- **요일별 일치율** 막대
- **활동별 주간 합계** — 활동마다 계획/실제 두 줄 막대와 주간 증감
- **시간대별 이탈 빈도** — 24개 막대. 그 시간대가 계획과 달랐던 **날 수**
  (한 시간 6칸 중 3칸 이상 어긋나면 그 날 이탈로 센다)
- **주간 가이드** — 요일·활동·시간대·총량 규칙 기반 문장

편집 화면에 떠 있는 오늘치는 아직 저장 전일 수 있으므로, 주간 데이터를 불러온 뒤
현재 화면 상태로 바꿔 끼워서 계산한다.

---

## 8. 단축키

| 키 | 동작 |
|---|---|
| `⌘S` / `Ctrl+S` | 지금 저장 |
| `⌘Z` / `Ctrl+Z` | 되돌리기 |
| `⌘W` / `Ctrl+W` | 주간 요약 열기·닫기 |
| `←` `→` | 어제/내일 (주간 요약에서는 지난 주/다음 주) |
| `Esc` | 활동 편집창 → 주간 요약 순서로 닫기 |

`e.metaKey || e.ctrlKey` 로 처리해 macOS/Windows 양쪽에서 같이 동작한다.
저장은 변경 후 0.7초 자동 저장 + `저장` 버튼(대기 중인 자동 저장을 취소하고 즉시
저장). 저장되면 상태 표시가 `저장됨 07:26` 처럼 시각까지 보여준다.

---

## 9. 기술 스택 / 구조

**정적 파일(HTML+CSS+JS) + Firebase(Firestore/Auth). 빌드 도구 없음.**

```
study_plan/
├── CLAUDE.md
├── docs/                     # GitHub Pages 로 배포되는 전체
│   ├── index.html
│   ├── style.css
│   ├── app.js                # 화면 로직 + Firestore 데이터 계층
│   ├── firebase-config.js    # Firebase 프로젝트 설정값 (공개돼도 안전)
│   ├── firestore.rules       # Firebase 콘솔에 붙여넣는 보안 규칙
│   └── README.md             # Firebase/GitHub Pages 설정 절차
└── 가이드/
    └── 배포 여정.html         # 전체 배포 과정을 그림과 함께 정리한 오프라인 안내서
```

- **가족 구분**: 로그인 없이, 무작위로 만든 긴 코드(`familyId`) 하나가 가족
  하나를 나타낸다. `?f=코드` 링크를 열면 그 가족 공간으로 들어간다.
  Firestore 문서 경로: `families/{familyId}/days/{date}`,
  `families/{familyId}/meta/activities`.
- **인증**: Firebase 익명 인증만 사용(로그인 화면 없음). Firestore 보안 규칙은
  "로그인한 사람만" 허용 — 실제 접근 통제는 무작위 familyId 가 담당한다
  (긴 비밀 링크 공유 모델, 구글 문서 "링크 아는 사람에게 공유"와 동일한 성격).
- `firebase-config.js`의 `apiKey` 등은 비밀값이 아니라 원래 공개되는 값이라
  공개 저장소에 커밋해도 안전하다 (Firebase 표준 방식).
- 실시간 자동 반영(`onSnapshot`)은 아직 없다 — 상대방의 최신 기록을 보려면
  새로고침이 필요하다. 의도적으로 범위를 좁혀 안정성을 우선했다.

### 데이터 계층 함수 (`docs/app.js`)
`apiGetDay` / `apiSaveDay` / `apiGetRange` / `apiGetRecent` / `apiGetActivities` /
`apiSaveActivities` — 전부 `db.doc(path).get()/.set()` 을 감싼 얇은 래퍼.

하루치 문서 형태:
```json
{
  "date": "2026-08-16",
  "plan":   [{ "start": 420, "end": 480, "activity": "math" }],
  "actual": [{ "start": 430, "end": 470, "activity": "math" }],
  "memo": "오후에 집중이 흐트러짐",
  "updated_at": "2026-08-16T21:10:00.000Z"
}
```
`start`/`end`는 자정 기준 **분(minute)** 값(0~1440). `activity`는 활동의 `id`.

### 개발 중 미리보기
```bash
cd study_plan/docs
python3 -m http.server 8000
```
`firebase-config.js` 를 먼저 채워야 정상 동작한다(비어 있으면 "연결할 수
없습니다" 화면).

### 소스를 고친 뒤
`docs/` 는 정적 파일이라 빌드 단계가 없다. `git add -A && git commit && git push`
하면 GitHub Pages 가 몇 분 안에 알아서 다시 배포한다. 새 파일을 추가했다면
브라우저 캐시 때문에 옛 화면이 보일 수 있으니 새로고침(⌘R/Ctrl+R)을 안내한다.

---

## 10. 연혁

- **Phase 1~2** — 로컬 데스크톱판(Python `http.server` + 더블클릭 `.app`/`.bat`)으로
  시작해 하루 계획표와 주간 요약을 완성했다.
- **Phase 3** — 가족이 함께 보는 **온라인 공유판**(`docs/`, Firebase+GitHub Pages)을
  추가로 만들었다.
- **Phase 4(현재)** — "앞으로 온라인 공유판만 쓴다"는 결정에 따라 로컬
  데스크톱판(`app.py`, `static/`, `tools/`, 빌드 산출물)을 **완전히 삭제**했다.
  지금은 `docs/` 하나만 유지·배포한다.
- 이번 라운드에 추가한 것: 활동 목록을 수학/영어/국어/생물/물리/화학/식사/
  취미/휴식/이동 10종으로 교체, 원 위에 활동 이름 라벨 표시, 라벨 클릭으로
  이름·색 즉시 편집.

**다음 후보**
- 요일 계획 템플릿 복사 (평일 템플릿 → 월~금 일괄 적용)
- 주간 리포트 내보내기 (HTML/PDF)
- 월간 추세 그래프 / CSV 내보내기
- 활동 그룹(공부/생활) 편집 UI, 활동 삭제
- Firestore `onSnapshot` 실시간 반영

---

## 11. 확정된 사항

1. 하루 범위 → **0시 시작 24시간** (자정을 넘겨 드래그하면 두 블록으로 자동 분할)
2. 최소 입력 단위 → **10분**
3. 로컬 데스크톱판은 폐기, **온라인 공유판만 유지**
