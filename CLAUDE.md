# 시후의 Step-UP! (Study Plan Circle)

24시간 원형 생활계획표 위에 **계획(Plan)** 과 **실행(Actual)** 을 나란히 기록하고,
그 차이를 분석해서 다음 계획을 개선하는 도구. **가족이 함께 보는 온라인 웹 앱**
하나로만 운영한다 (로컬 데스크톱판은 폐기했다 — [10. 연혁](#10-연혁) 참고).

앱 최상단은 **"시후의 Step-UP!"** 타이틀 + 그 아래 **탭 바**(`.app-tabs`)로
구성된다. 탭은 네 개다 — 하루 계획표 · 주간 일정표 · 학습진도 현황판 ·
수행평가. 탭은 열고 닫는 오버레이가 아니라 같은 화면 안의 서로 다른 페이지를
오가는 개념이며, 전환은 `switchTab(tab)` 하나가 담당한다. 탭 정의는
`APP_TABS` 표에 모여 있어 **표에 한 줄만 보태면 탭이 늘어난다**.
반면 "주간 요약"은 여전히 날짜별 하루 화면 위에 뜨는 **오버레이**다(탭이 아님).

---

## 1. 목적

- 하루 계획을 원 그래프(24시간 시계형)로 직관적으로 입력한다.
- 같은 계획표를 오른쪽에 복사해 두고, 그 위에 **실제 실행**을 겹쳐서 기록한다.
- 계획 대비 실행의 차이(활동별 시간, 시간대별 일치율)를 수치와 그래프로 보여준다.
- 이 데이터를 근거로 "계획을 어떻게 수정할지" 가이드를 얻는다.
- 시후와 부모님이 **각자 기기에서 같은 링크**로 열어 같은 기록을 본다.

---

## 2. 화면 구성

```
┌───────────────────────────────────────────────────────────────────────────┐
│  시후의 Step-UP!    [시후의 하루 계획표] [주간 일정표]             [공유]  │  ← 최상단 헤더 + 탭
├───────────────────────────────────────────────────────────────────────────┤
│  [◀] 2026-08-16 (일) [▶] [오늘]              [저장][되돌리기][주간 요약]  │  ← .day-toolbar (탭별로 내용 다름)
├──────────────────────────────┬────────────────────────────────────────────┤
│         📘 계획 (Plan)        │          ✅ 실행 (Actual)                  │
│                              │                                            │
│         ┌──────────┐         │          ┌──────────┐                      │
│         │  24시간   │         │          │ 계획(흐림) │  ← 계획이 배경으로   │
│         │  원그래프  │         │          │ +실행(진함)│     그대로 복사됨   │
│         │ 이름 라벨  │         │          │ 이름 라벨  │  ← 블록 위에 이름   │
│         └──────────┘         │          └──────────┘     표시, 클릭해 편집 │
│                              │                                            │
│  [활동 팔레트]                │  [활동 팔레트]                              │
│  ● 수학 ● 영어 ● 국어 ...     │  ● 수학 ● 영어 ● 국어 ...                   │
│                              │  [계획 그대로 복사 ⧉]                       │
│  시간 블록 리스트 (편집/삭제)   │  시간 블록 리스트 (편집/삭제)                │
├──────────────────────────────┴────────────────────────────────────────────┤
│  📊 계획 vs 실행 분석                                                       │
│  일치율 78%  |  활동별 계획/실제 막대 비교  |  자동 코멘트(개선 가이드)        │
└───────────────────────────────────────────────────────────────────────────┘
```

- 위 그림은 **"시후의 하루 계획표"** 탭(`#panelDay`)이 활성화된 모습이다.
  **"주간 일정표"** 탭(`#panelSchedule`)을 누르면 이 영역 전체가 요일×시간
  격자표(8-1 참고)로 바뀐다 — 같은 화면 안에서 페이지만 바뀌는 것이지,
  오버레이가 뜨는 게 아니다.
- 왼쪽/오른쪽 원은 **같은 크기, 같은 좌표계**. 오른쪽 원에는 계획이 항상
  옅은 톤(투명도 42%)으로 전체에 깔려 있고, 실행 기록이 그 위에 **안쪽 진한
  띠**(전체 반지름의 3/5)로 겹쳐진다.
- 화면이 좁으면(<1100px) 위/아래로 자동 스택.

---

## 3. 원 그래프 규격

| 항목 | 값 |
|---|---|
| 형태 | 24시간 시계형 원 (0시 = 12시 방향, 시계방향) |
| 1시간 | 15° |
| 최소 입력 단위 | 10분 (스냅) |
| 계획 표현 | 꽉 찬 부채꼴(파이) |
| 실행 표현 | 안쪽 진한 띠, 눈에 보이는 면적이 전체 원의 3/5이 되도록 반지름을 √(3/5)배로 계산(단순 반지름 3/5배는 면적이 36%로 작아 보임). 계획은 전체에 옅게 깔려 바깥쪽에서 비쳐 보임 |
| 이름 라벨 | 안쪽 원과 바깥 원 사이 정중앙에 표시. 50분 이상은 가로로, 22~50분은 그 블록의 반지름 방향(안쪽 원→바깥 원)을 "세로"로 보고 한 글자씩 쌓아서(원 위쪽 절반은 순서 반전, 항상 화면 위→아래로 올바르게 읽힘), 22분 미만은 점선을 원 바깥 여백까지 끌어내 그 끝에 이름을 적는 "콜아웃"으로 표시한다. 인라인 글자색은 배경색 밝기에 따라 흰/검정 자동 선택, 콜아웃은 점선을 활동 색으로 그려 어느 블록인지 잇는다 |
| 눈금 | 매시 눈금, 3시간마다 숫자(0,3,6,9,12,15,18,21) |
| 렌더링 | SVG (선명하고 클릭/드래그 좌표 계산이 정확함) |

주간 요약의 요일별 미니 원그래프에는 이름 라벨을 넣지 않는다(너무 작아 안 보임).

---

## 4. 입력 방식

1. **원 위 드래그** — 활동 팔레트에서 활동 하나를 선택한 뒤, 원 위에서 시작 각도부터
   끝 각도까지 드래그하면 그 구간이 그 활동 색으로 칠해진다. 드래그 중에는
   커서 옆에 `07:30 → 09:00 (1시간 30분)` 툴팁이 실시간 표시된다.
2. **블록 편집** — 오른쪽 리스트에서 시작/종료 시간을 select 박스로 직접 수정, 삭제.
3. **겹침 처리** — 새 블록이 기존 블록과 겹치면 **새 블록이 이깁니다**(기존 블록이
   잘리거나 분할됨). 되돌리기(⌘Z/Ctrl+Z) 지원.
4. **지우개** — 팔레트의 지우개를 선택하고 드래그하면 그 구간이 비워진다.
5. **실행 쪽 전용** — `계획 그대로 복사 ⧉` 버튼 한 번으로 계획을 실행 쪽에 통째로
   복사한 뒤, 달라진 부분만 고쳐 그리면 된다(가장 빠른 기록 방법).
6. **지금 기록** — 실행 쪽에 "지금까지 ○○ 함" 버튼: 마지막 기록 시각 ~ 현재 시각을
   선택한 활동으로 채운다.
7. **이름·색 편집** — 원 위에 표시된 활동 이름 글자를 클릭하면 편집창이 뜨고,
   이름과 색을 바로 고칠 수 있다. 저장하면 그 활동을 쓰는 모든 블록(과거 기록
   포함)에 새 이름·색이 한꺼번에 반영된다. 이름 글자를 클릭하는 것이므로,
   글자가 없는 짧은 블록(50분 미만)은 팔레트에서 눌러 고를 순 있어도 원 위
   클릭으로는 편집할 수 없다 — 그 활동이 들어간 다른 긴 블록이나, 이 활동으로
   새로 그려서 늘린 뒤 편집하면 된다.
8. **활동 관리** — 팔레트의 `⚙ 관리` 버튼을 누르면 활동 전체 목록이 뜨고,
   블록을 먼저 그리지 않아도 이름·색을 바꾸거나 새로 추가하거나(하단 입력폼)
   `×` 로 삭제할 수 있다. 삭제해도 이미 그려둔 블록은 안 지워지지만, 그 활동의
   id(예: `play`)가 그대로 이름처럼 보인다 — 삭제 전 확인창에서 미리 알려준다.
   "활동 하나를 두 개로 나누기"는 기존 활동 이름을 하나로 바꾸고(예: `국어/독서`
   → `국어`) 새 활동을 하나 추가하는(`독서`) 방식으로 한다 — 과거에 그려둔
   블록은 나눌 수 없으므로 이 방식이 기록을 안 잃는 유일한 방법이다.

---

## 5. 활동(카테고리)

기본 제공 10종 — 이름/색 모두 원 위 라벨을 클릭해 언제든 바꿀 수 있다.
Firestore의 `families/{familyId}/meta/activities` 문서에 저장된다.

| 활동 | 색 | 그룹 |
|---|---|---|
| 수학 | `#4C6FFF` | 공부 |
| 영어 | `#8B5CF6` | 공부 |
| 국어 | `#14B8A6` | 공부 |
| 생물 | `#22A559` | 공부 |
| 물리 | `#0EA5E9` | 공부 |
| 화학 | `#D6409F` | 공부 |
| 식사 | `#F2B134` | 생활 |
| 취미 | `#F2994A` | 생활 |
| 휴식 | `#9AA5B1` | 생활 |
| 이동 | `#C7CDD6` | 생활 |

`＋ 활동` 으로 새 활동을 추가하면 기본 그룹은 `공부`다. 원 가운데 "공부 시간"
합계는 `group: "공부"` 인 활동들만 더한다 — 생활 활동으로 두고 싶으면 편집창
(라벨 클릭)에서 이름/색만 바꿀 수 있고, 그룹은 아직 UI로 못 바꾼다(필요하면
Firestore 콘솔에서 `meta/activities` 문서의 해당 항목 `group` 값을 직접 고친다).

---

## 6. 분석 (하단 패널)

- **시간 일치율(%)** — 10분 슬롯 144칸 기준, 계획 활동 == 실행 활동인 슬롯 비율.
- **공부 시간 총합** — 계획 vs 실제 (예: `계획 5h 20m → 실제 4h 10m, -1h 10m`).
- **활동별 비교 막대** — 활동마다 계획/실제 두 줄 막대와 증감(±분).
- **가장 많이 어긋난 시간대 Top 3** — 예: `16:00–17:00 계획 "수학" → 실제 "취미"`.
- **자동 코멘트(가이드)** — 규칙 기반 문장. 예시:
  - 계획 대비 실제가 계속 짧은 공부 활동 → "물리는 3일 연속 계획보다 20분 이상
    짧습니다. 한 블록을 40분으로 줄이고 쉬는 시간을 사이에 넣어보세요."
  - 특정 시간대 이탈 반복 → "16시대는 3일 연속 계획대로 되지 않았습니다."
  - 계획에 없던 활동이 반복 등장 → "계획에 없던 취미가 3일 나타났습니다."
- 메모칸 1개(그날 한 줄 회고).

---

## 7. 주간 요약 대시보드

헤더의 `주간 요약`(또는 `⌘W`/`Ctrl+W`)으로 열리는 전체 화면 대시보드.
주는 **월요일 시작**이며 `‹ ›` 또는 `←`/`→` 로 주 단위 이동한다.

- **KPI 4종** — 주간 공부 시간(계획 대비 증감), 평균 일치율, 기록한 날 수,
  가장 잘 지킨 요일
- **요일별 미니 원그래프 7개** — 안쪽 실행 / 바깥 계획(라벨 없음). 오늘은 테두리
  강조, 클릭하면 그 날짜의 편집 화면으로 이동한다.
- **요일별 일치율** 막대
- **활동별 주간 합계** — 활동마다 계획/실제 두 줄 막대와 주간 증감
- **시간대별 이탈 빈도** — 24개 막대. 그 시간대가 계획과 달랐던 **날 수**
  (한 시간 6칸 중 3칸 이상 어긋나면 그 날 이탈로 센다)
- **주간 가이드** — 요일·활동·시간대·총량 규칙 기반 문장

편집 화면에 떠 있는 오늘치는 아직 저장 전일 수 있으므로, 주간 데이터를 불러온 뒤
현재 화면 상태로 바꿔 끼워서 계산한다.

---

## 8. 단축키

| 키 | 동작 |
|---|---|
| `⌘S` / `Ctrl+S` | 지금 저장 |
| `⌘Z` / `Ctrl+Z` | 되돌리기 (하루 계획표 탭에서만) |
| `⌘W` / `Ctrl+W` | 주간 요약 열기·닫기 (하루 계획표 탭에서만) |
| `←` `→` | 어제/내일 — 하루 계획표 탭에서만 동작(주간 요약 오버레이가 열려 있으면 지난 주/다음 주). **주간 일정표 탭에서는 아무 동작 없음**(입력칸 안에서 커서만 움직인다) |
| `Esc` | 활동 편집창 → 활동 관리 → 주간 요약 순서로 닫기 |

`e.metaKey || e.ctrlKey` 로 처리해 macOS/Windows 양쪽에서 같이 동작한다.
저장은 변경 후 0.7초 자동 저장 + `저장` 버튼(대기 중인 자동 저장을 취소하고 즉시
저장). 저장되면 상태 표시가 `저장됨 07:26` 처럼 시각까지 보여준다.

단축키 스코프는 `switchTab()`이 여닫는 `#panelDay`의 `hidden` 여부
(`dayTabActive`)로 판별한다 — 주간 일정표 탭은 오버레이가 아니라 별개
페이지이므로, 더 이상 "열려 있는 오버레이" 목록에 들어가지 않는다.

---

## 8-1. 주간 일정표 (`주간 일정표` 탭)

원그래프로 하루하루 계획/실행을 겹쳐 기록하는 것과는 **완전히 별개**의 기능이며,
"시후의 하루 계획표"와 동등한 레벨의 **최상단 탭**(`#tabBtnSchedule` →
`#panelSchedule`)으로 존재한다. 예전에는 열고 닫는 오버레이였지만, 앞으로
탭이 더 늘어날 걸 감안해 페이지 전환 방식(`switchTab()`)으로 바뀌었다 —
"닫기" 버튼이 없고, 다른 탭으로 옮기면 그걸로 끝이다.
매주 반복되는 요일×시간 시간표를 표 형식으로 세워두고 참고하는 용도.

- **구조** — 시간(27칸, 7:00~24:00을 일과 단위로 쪼갠 불규칙한 간격) × 요일(월~일)
  의 표. 열 너비는 `colgroup` + `table-layout: fixed` 로 못 박는다(시간 88px,
  요일 66px → 표 550px). 표를 늘리지 않고 이 폭으로 고정해야 두 주를 나란히
  놓을 수 있다. 좁은 화면에서는 칸이 짓눌리는 대신 `min-width: 550px` 로
  가로 스크롤이 걸린다.
- **이번 주·다음 주를 나란히**(`.sched-pair`) — 실제로 계획을 세우는 건 이 둘
  뿐이라, 왼쪽에 이번 주·오른쪽에 다음 주를 놓고 나머지는 아래로 내린다.
  1240px 아래로 좁아지면 자연스럽게 위아래로 쌓인다.
  - **1교시~7교시와 점심시간은 한 교시가 한 줄**이다. 학교가 정한 대로라 어차피
    못 바꾸는 시간이어서, 30분씩 두 줄로 쪼개 봐야 같은 글자만 두 번 나오고
    표만 길어진다(35칸 → 27칸).
  - 슬롯을 지울 때는 **남길 경계의 시작분을 그대로 둔다.** `slot.id` 가 곧
    칸 데이터의 키라서, 남긴 칸은 예전 입력을 그대로 이어받는다. 지워진
    경계에 붙어 있던 값(예: 옛 `월_570`)은 데이터에는 남지만 화면에 안 나온다
    — 지우지 않으니 되돌릴 여지는 남아 있다.
- **편집판도 같은 값끼리 묶어서 보여준다**(`schedSpans()`) — 기본값이 거의
  안 바뀌는 표라, 같은 글자가 세로 두 줄·가로 월~금 반복되는 것보다 한 칸으로
  묶어 보이는 게 훨씬 읽기 쉽다. 묶인 칸은 입력칸 하나이고, **거기 쓴 내용은
  묶인 범위 전체에 그대로 들어간다**.
  - **빈 칸은 묶지 않는다.** 안 그러면 토/일 빈칸이 통째로 한 덩어리가 돼서
    특정 요일에만 뭘 적어 넣을 수가 없다.
  - 특정 요일만 다르게 하려면 묶인 칸의 **나누기(⇹)** 버튼으로 떼어낸 뒤
    고친다(`scheduleSplit` Set — 화면 상태일 뿐 저장하지 않는다. 값이 실제로
    달라지면 그 뒤로는 자연히 안 묶인다).
  - 잘못 눌렀으면 **다시 합치기(⇼)** 로 되돌린다 — 붙어 있는 같은 값 칸들
    (`schedSameRegion()` 의 flood fill)의 나눔 표시를 한꺼번에 지운다.
    두 버튼 다 항상 보인다(반투명 → 칸에 마우스 올리면 진해짐).
  - **드래그로 합치기**(`attachScheduleDrag()`) — 표 위를 끌면 지나간 사각형
    범위가 대표값(범위 안 첫 내용) 하나로 채워지면서 한 칸이 된다. 칸이 전부
    입력칸이라 누르는 순간엔 클릭인지 드래그인지 알 수 없어서, 6px 넘게
    움직였을 때 비로소 드래그로 보고 입력 포커스를 뺀다.
    **터치는 제외한다** — 스크롤하려고 쓸어넘긴 것까지 합치기로 오해하면
    손대지도 않은 칸이 통째로 바뀐다. 터치에서는 버튼으로만 합친다.
  - 내용을 고쳤다고 표를 다시 그리지는 않는다. 묶인 칸을 고쳐도 묶인 범위는
    그대로라 다시 그릴 이유가 없고, 칸을 옮겨다니며 입력할 때 포커스만 끊긴다.
- **기본값** — `SCHED_DEFAULT_FILLS`(`docs/app.js`)에 기숙학교 일과 기준으로
  미리 채워져 있다. `＋ 새 주`를 누르면 이 기본값으로 채워진 새 표가 위에
  추가되고, 지난 주들은 아래로 계속 남아 스크롤해서 언제든 다시 볼 수 있다.
  "새 주"는 항상 이 고정 기본값에서 다시 시작하며, 직전 주에 고친 내용을
  이어받지 않는다.
- **지난 주는 세 단계로 접힌다** — 몇 주만 쌓여도 표를 다 펼쳐 두면 스크롤이
  끝없이 길어지는데, 실제로는 어느 주를 볼지 고른 뒤에야 내용이 필요하다.
  1. **한 줄로 접힘**(기본) — `W32 · 2026. 8. 3. – 8. 9. · 1주 전` 한 줄.
     머리글 줄 전체가 스위치라 아무 데나 누르면 펼쳐진다(`scheduleOpen`).
     줄 안의 버튼을 누른 건 펼치기로 치지 않는다.
  2. **요약판**(`schedSummaryHtml()`) — 읽기 전용. 아래 규칙대로 묶어서 보여준다.
  3. **편집판** — `펼쳐서 편집` 을 누르면(`scheduleExpanded`). 다시 접으면
     편집 상태도 같이 풀려, 다음에 펼칠 때는 요약부터 시작한다.
  - **세로 묶음**(`schedGroups()`) — 7요일 값이 **전부 같은 상태로 이어지는**
    시간 칸들을 한 줄로 합치고, 시간 라벨은 합쳐진 구간 전체(`9:00~10:00`)로
    표시한다.
  - **가로 묶음** — 그 줄 안에서 값이 같은 이웃 요일을 `colspan` 으로 합친다
    (월~금이 모두 "1교시"면 한 칸). 화/목만 다르면 거기서 자동으로 쪼개진다.
  - 아무 값도 없는 구간은 시간만 남긴 얇은 줄(`tr.sc-gap`)로 눌러서, 시간
    흐름은 끊기지 않으면서 높이는 거의 차지하지 않게 한다.
  - 실제로 35줄짜리 표가 20줄 안팎으로 줄어든다. 고쳐야 하면 머리글의
    **`펼쳐서 편집`** 버튼으로 그 주만 편집판으로 펼친다(`scheduleExpanded`
    Set — 화면 상태일 뿐 저장하지 않는다).
- **시간 칸도 편집 가능** — 맨 왼쪽 시간 칸(`.sc-time-in`)에 `7:00~7:30` 형식으로
  직접 입력한다(`parseSchedTime()`). **바로 옆 한 칸의 길이만 늘거나 줄고,
  나머지 칸은 건드리지 않는다** — 끝시간을 미루면 다음 칸이 그만큼 짧아지고,
  당기면 길어진다(시작시간을 고치면 앞 칸이 같은 식으로 반응). 표 전체를
  밀어버리면 아래쪽 일과 시각까지 다 어긋나므로 일부러 옆 한 칸으로 막아둔다.
  읽을 수 없는 글자거나, 옆 칸을 0분 이하로 뭉개거나, 마지막 칸이 24:00을
  넘기는 값이면 **받지 않고 원래 값으로 되돌린다**.
- **저장** — 칸을 고치고 포커스를 벗어나면(change) 0.7초 뒤 자동 저장.
  Firestore 문서 경로: `families/{familyId}/scheduleWeeks/{주시작일}`,
  `{ weekStart, cells: { "월_540": "1교시", ... }, slots?, updated_at }` 형태.
  `cells`의 키는 `요일_슬롯id` (예: `월_540` = 월요일 9:00 칸).
  - `slots`는 **시간을 한 번이라도 고친 주에만** 붙는다(`[{id,start,end}]`).
    없으면 기본 시간표(`SCHED_SLOTS`)를 쓰므로 예전 데이터도 그대로 열린다
    (`slotsOf()`).
  - **`slot.id` 는 절대 바뀌지 않는다.** 칸 데이터의 키에 이미 박혀 있어서,
    시간을 고칠 때 움직이는 건 `start`/`end` 뿐이다 — id 를 현재 시작분에서
    다시 만들면 그 주의 입력 내용이 통째로 미아가 된다.
- **목록 조회** — `db.collection(...).orderBy('weekStart','desc').get()` 로
  전체 주를 한 번에 불러온다(이 프로젝트에서 컬렉션 쿼리를 쓰는 유일한 곳 —
  나머지는 전부 `db.doc()` 단건 조회). Firestore 보안 규칙은 문서 단위로
  적용되므로 기존 `families/{familyId}/{document=**}` 규칙이 그대로 커버한다.
- **"이번 주/n주 전/n주 후" 배지** — 목록 순서가 아니라 실제 오늘 날짜와
  각 주 시작일의 날짜 차이로 계산한다(`scheduleWeekBadge()`).
- **주차 표시** — 머리글 맨 앞에 `W33` 형태의 ISO 8601 주차를 붙인다
  (`isoWeekNo()` — 그 주 목요일이 속한 해의 몇 번째 주인지로 센다).
- **주 삭제** — 머리글의 `삭제` 버튼. `＋ 새 주`를 잘못 눌렀을 때 되돌리기
  위한 것이라 되돌릴 수 없다는 확인을 한 번 받고 지운다(`confirm`).
- **칸 색은 "성격"으로 묶는다**(`SCHED_CATEGORIES` / `schedCategory()`) —
  글자마다 색을 따로 주면 표가 무지개가 돼서 오히려 안 읽힌다. 1교시~7교시는
  전부 `class` 한 색, 식사·간식은 `meal` 한 색 하는 식으로 7가지로 줄였다.
  - 규칙은 **위에서부터 먼저 걸리는 게 이긴다.** "4교시(자습)"은 자습이 아니라
    수업이고 "Roll-Call / 취침"은 점호가 아니라 잠자리라서, 순서가 곧 의미다.
  - 색은 CSS 의 `td[data-cat=...]` 에서 `--c-*` 토큰으로 정한다. 라이트/다크
    각각 따로 골라 뒀다.
  - 분류에 안 걸리는 내용만 글자 해시색(`schedHue()` → `--cell-h`)으로 떨어진다.
- **Enter 로 확정 + 표 계산기식 이동** — 표 안 입력칸은 감싸는 폼이 없어서
  Enter 가 그냥 무시된다. keydown 을 받아 직접 확정하고, 커서를 **아래 칸으로
  넘긴다**(맨 아랫줄이면 제자리). 방향키로도 칸 사이를 오간다.
  - 확정은 blur 때 오는 `change` 에만 기대지 않고 `commitCell()` /
    `commitTime()` 을 직접 부른다 — `change` 는 브라우저가 "사용자가 고쳤다"고
    표시한 입력칸에서만 오기 때문.
  - 값이 바뀌면 묶임·색이 달라져 표를 다시 그려야 하는데, 그러면 입력칸이
    통째로 새로 만들어져 커서가 날아간다. 그래서 갈 자리를 `scheduleFocusTarget`
    에 적어 두고 `renderScheduleWeeks()` 끝에서 `applyScheduleFocus()` 로 다시
    잡는다.
  - **좌우 화살표는 글자 안에서 커서를 옮기는 중이면 가로채지 않는다.** 다만
    방금 건너와 글자가 통째로 선택된 상태면 칸 이동으로 본다(표 계산기와 같다).
  - 묶인 칸에서 아래로 가면 그 덩어리 **바깥**(`data-r2 + 1`)으로 나간다.
  - 시간 칸은 값이 안 읽히면(`'rejected'`) 고칠 수 있도록 커서를 제자리에 둔다.
  - Esc 는 고치던 걸 물린다.
- **머리글·시간 열 고정** — 35줄짜리 표에서 지금 보는 칸이 무슨 요일인지
  놓치지 않도록 `position: sticky` 로 붙여 둔다(표 감싸개에 `max-height` 를
  줘야 세로 sticky 가 걸린다). 요약판은 읽기 전용이라 통째로 펼쳐 보여준다.

---

## 8-2. 학습진도 현황판 (`학습진도 현황판` 탭)

과목마다 끝내야 할 **교재·부교재·단어장**을 두고 진도를 채워 나가는 판.
하루 계획표의 활동·공부 시간과는 **엮지 않는다**(의도적으로 분리했다).

- **과목 목록은 수행평가와 공유**한다(`meta/subjects`). 하루 계획표의 활동
  목록(`meta/activities`)과는 별개다 — 학교 과목과 하루 일과는 다른 축이라
  하나로 묶으면 양쪽 다 지저분해진다.
- **항목 = 교재 한 권**(`progressItems/{id}`). 과목이 아니라 교재를 단위로
  잡은 이유: "수학 60%"는 뜻이 모호하지만 "쎈 수학(상) 186/300쪽"은 명확하다.
  단위는 쪽·단원·강·문항·개 중에서 고른다.
- **목표는 총 분량 + 목표일 두 가지**를 받는다. 이 둘이 있어야 단순 진행률을
  넘어 계산이 나온다(`itemPace()`):
  - `perDay` — 남은 분량 ÷ 남은 날
  - `diff` — 지금까지의 실제 속도(현재진도 ÷ 경과일)로 남은 분량을 끝낼 때
    걸리는 날을 재서, 목표일까지 남은 날과의 차이. 양수면 빠름, 음수면 늦음.
  - 이게 이 판의 핵심이다. 진행바만 있으면 "62% 했네"에서 끝나지만
    "이 속도면 5일 늦어요"는 행동을 바꾼다.
- **진도 입력은 숫자 하나**("지금 186쪽"). 고칠 때마다 `history` 에 날짜와
  값을 남긴다 — 속도 계산이 이 기록에 기댄다.
- 끝낸 항목은 과목 섹션 맨 아래 `끝낸 항목 n개` 한 줄로 접는다.
- 줄 끝의 **✕** 로 바로 지운다(편집창 안에도 삭제가 있지만, 지우려고 편집창까지
  들어가야 하는 건 번거롭다). 되돌릴 수 없으니 확인을 한 번 받는다.

---

## 8-3. 수행평가 (`수행평가` 탭)

제출/미제출 두 상태뿐이고 **기한을 넘기면 위험한** 과제를 따로 관리한다.
진도판과 성격이 완전히 달라 탭을 나눴다(`tasks/{id}`).

- **진행바가 아니라 D-day**를 쓴다. 부분 진행이 없으니 실제로 알아야 할 건
  "며칠 남았나" 하나다.
- **급한 정도를 `taskLevel()` 하나로 정한다** — 지남(0) / D-3 이내(1) /
  D-10 이내(2) / 그 외(3). 정렬도 색도 전부 이 값에서 나온다.
- **과목별로 묶되, 과목 섹션의 순서를 그 과목에서 가장 급한 수행이 정한다.**
  그냥 과목으로만 묶으면 급한 게 화면 아래로 흩어져서, 기한 넘김을 막자는
  목적이 무너진다. 순서를 기한이 정하게 하면 묶음을 유지하면서도 급한 게
  항상 위로 온다. 시험 기간용으로 `기한순` 전환도 둔다.
- **급한 항목을 위에 따로 복사해 두지 않는다.** 같은 항목이 두 번 보이면
  "이거 아까 제출했나?" 하는 혼란이 생긴다. 맨 위 경고 줄은 개수와 이름만
  알려주는 안내이지 누르는 목록이 아니다.
- **탭 배지**(`refreshTaskBadge()`) — 기한 지남 + D-3 이내 미제출 건수.
  다른 탭에 있어도 보여야 하므로 **앱을 열 때 수행 목록만 미리 읽는다**
  (진도판은 그 탭에 들어갈 때 읽어도 늦지 않다).
- 제출한 수행은 맨 아래 한 줄로 접고, 펼쳐서 되돌릴 수 있다.
- 줄 끝의 **✕** 로 바로 지운다(진도판과 같다).

### 편집창을 넓힐 때 주의

기본 `.edit-act-card` 에는 `max-width: 300px` 가 걸려 있다. `.wide` 에서
`width` 만 키우면 max-width 가 이겨서 **칸이 카드를 뚫고 나간다.** max-width
자체를 다시 잡아야 한다. 그리고 격자는 `minmax(0, 1fr)` 로, `select`·`date`·
`number` 에는 `width: 100%` 를 직접 줘야 한다 — 손대지 않으면 제 내용만큼
넓어져 칸을 넘는다(`1fr` 은 `min-width: auto` 라 막아주지 못한다).

---

## 9. 기술 스택 / 구조

**정적 파일(HTML+CSS+JS) + Firebase(Firestore/Auth). 빌드 도구 없음.**

```
study_plan/
├── CLAUDE.md
├── docs/                     # GitHub Pages 로 배포되는 전체
│   ├── index.html
│   ├── style.css
│   ├── app.js                # 화면 로직 + Firestore 데이터 계층
│   ├── firebase-config.js    # Firebase 프로젝트 설정값 (공개돼도 안전)
│   ├── firestore.rules       # Firebase 콘솔에 붙여넣는 보안 규칙
│   └── README.md             # Firebase/GitHub Pages 설정 절차
└── 가이드/
    └── 배포 여정.html         # 전체 배포 과정을 그림과 함께 정리한 오프라인 안내서
```

- **가족 구분**: 로그인 없이, 무작위로 만든 긴 코드(`familyId`) 하나가 가족
  하나를 나타낸다. `?f=코드` 링크를 열면 그 가족 공간으로 들어간다.
  Firestore 문서 경로: `families/{familyId}/days/{date}`,
  `families/{familyId}/meta/activities`.
- **인증**: Firebase 익명 인증만 사용(로그인 화면 없음). Firestore 보안 규칙은
  "로그인한 사람만" 허용 — 실제 접근 통제는 무작위 familyId 가 담당한다
  (긴 비밀 링크 공유 모델, 구글 문서 "링크 아는 사람에게 공유"와 동일한 성격).
- `firebase-config.js`의 `apiKey` 등은 비밀값이 아니라 원래 공개되는 값이라
  공개 저장소에 커밋해도 안전하다 (Firebase 표준 방식).
- 실시간 자동 반영(`onSnapshot`)은 아직 없다 — 상대방의 최신 기록을 보려면
  새로고침이 필요하다. 의도적으로 범위를 좁혀 안정성을 우선했다.

### 데이터 계층 함수 (`docs/app.js`)
`apiGetDay` / `apiSaveDay` / `apiGetRange` / `apiGetRecent` / `apiGetActivities` /
`apiSaveActivities` — 전부 `db.doc(path).get()/.set()` 을 감싼 얇은 래퍼.

하루치 문서 형태:
```json
{
  "date": "2026-08-16",
  "plan":   [{ "start": 420, "end": 480, "activity": "math" }],
  "actual": [{ "start": 430, "end": 470, "activity": "math" }],
  "memo": "오후에 집중이 흐트러짐",
  "updated_at": "2026-08-16T21:10:00.000Z"
}
```
`start`/`end`는 자정 기준 **분(minute)** 값(0~1440). `activity`는 활동의 `id`.

### 개발 중 미리보기
```bash
cd study_plan/docs
python3 -m http.server 8000
```
`firebase-config.js` 를 먼저 채워야 정상 동작한다(비어 있으면 "연결할 수
없습니다" 화면).

### 소스를 고친 뒤
`docs/` 는 정적 파일이라 빌드 단계가 없다. `git add -A && git commit && git push`
하면 GitHub Pages 가 몇 분 안에 알아서 다시 배포한다. 새 파일을 추가했다면
브라우저 캐시 때문에 옛 화면이 보일 수 있으니 새로고침(⌘R/Ctrl+R)을 안내한다.

---

## 10. 연혁

- **Phase 1~2** — 로컬 데스크톱판(Python `http.server` + 더블클릭 `.app`/`.bat`)으로
  시작해 하루 계획표와 주간 요약을 완성했다.
- **Phase 3** — 가족이 함께 보는 **온라인 공유판**(`docs/`, Firebase+GitHub Pages)을
  추가로 만들었다.
- **Phase 4(현재)** — "앞으로 온라인 공유판만 쓴다"는 결정에 따라 로컬
  데스크톱판(`app.py`, `static/`, `tools/`, 빌드 산출물)을 **완전히 삭제**했다.
  지금은 `docs/` 하나만 유지·배포한다.
- 이번 라운드에 추가한 것: 활동 목록을 수학/영어/국어/생물/물리/화학/식사/
  취미/휴식/이동 10종으로 교체, 원 위에 활동 이름 라벨 표시, 라벨 클릭으로
  이름·색 즉시 편집.
- **Phase 5** — 원그래프 라벨을 안쪽/바깥쪽 원 가운데로 정렬하고, 좁은 구간은
  반지름 방향(안↔밖) 세로쓰기로, 그것도 부족하면 점선 콜아웃으로 빼는 3단계
  라벨 배치를 완성. 실행/계획의 시각 역할을 맞바꿔 실행을 안쪽 진한 띠로,
  넓이 기준 3/5를 차지하도록 확대(`√(3/5)` 반지름 스케일).
- **Phase 6** — "주간 일정표" 기능 추가: 요일×시간 고정 격자표에 기숙학교
  기본 일과를 미리 채워두고, `＋ 새 주`로 항상 같은 기본값에서 새 주를
  시작하며 지난 주는 아래로 계속 쌓이는 방식. 처음엔 열고 닫는 오버레이로
  넣었다가, **최상단 탭**(시후의 하루 계획표 / 주간 일정표)으로 재구성해
  "시후의 Step-UP!"이라는 앱 이름 아래 나란히 두는 구조로 바꿨다 — 탭이
  하나 더 늘어날 걸 감안해 `switchTab()` 라우터 하나로 확장 가능하게 설계.
- **Phase 7** — 지난 주를 읽기 전용 **요약판**(같은 구간을 가로·세로로 묶어
  35줄 → 20줄 안팎)으로 접어 두고 필요할 때만 펼쳐 편집하도록 바꿨다.
  또 시간 칸 자체를 편집할 수 있게 해서(`slots` 를 주별로 저장), 학교
  일과가 바뀌어도 그 주 시간표를 통째로 다시 만들 필요가 없어졌다.
- **Phase 8** — "기본값은 거의 안 바뀐다"는 전제에 맞춰, 묶어 보여주기를
  **편집판까지** 넓혔다(`schedSpans()` 의 직사각형 병합). 묶인 칸을 고치면
  묶인 범위 전체가 같이 바뀌고, 특정 요일만 떼어내고 싶을 때만 나누기(⇹)를
  쓴다. 요일 칸 너비도 줄여 한눈에 들어오게 했다.
- **Phase 9** — 표를 실제로 굴려 쓰면서 나온 요청들을 한 번에 반영했다:
  다시 합치기(⇼)와 드래그로 합치기, 시간 변경 시 옆 한 칸 늘이고 줄이기,
  칸 내용에 따른 파스텔 색, 주차(W33) 표시, 주 삭제 버튼. 기본값에서
  금요일 저녁 기숙사 일과(간식/샤워/취침)는 귀가라서 빼고 목요일까지만
  채우도록 고쳤다(`SCHED_DORM_DAYS`).
- **Phase 14** — 탭을 넷으로 늘려 **학습진도 현황판**과 **수행평가**를 붙였다.
  둘은 과목 목록만 공유하고 나머지는 완전히 별개다. 수행평가는 처음에 진도판
  안에 두려다, 성격(기한 있는 이분법 vs 꾸준히 채우는 진행률)이 너무 달라
  탭을 분리했다. 탭 정의를 `APP_TABS` 표로 모아 다음 탭은 한 줄로 늘어난다.
- **Phase 13** — 요일 칸 폭을 절반(66px)으로 줄이고, 이번 주와 다음 주를
  좌우로 나란히 놓았다. 지난 주는 아래에 **한 줄씩 접어** 쌓아, 몇 주가
  쌓여도 목록이 한 화면에 들어오게 했다.
- **Phase 12** — Enter 를 치면 커서가 사라지던 걸 고쳐, 아래 칸으로 넘어가고
  방향키로도 칸 사이를 오갈 수 있게 했다(표 계산기처럼).
- **Phase 11** — 1교시~7교시와 점심시간을 30분 두 줄에서 **한 교시 한 줄**로
  묶어 표를 35칸에서 27칸으로 줄였다. 남긴 경계의 시작분을 그대로 둬서 기존
  주의 입력이 그대로 이어진다.
- **Phase 10** — 칸 색을 글자별 해시에서 **일과 성격별 7색**으로 줄이고
  (1교시~7교시가 전부 다른 색이라 난잡했다), Enter 로 입력을 확정할 수 있게
  하고, 요일 머리글·시간 열을 스크롤에 고정했다. 주 단위 카드도 머리글과 표를
  한 몸으로 묶어 테두리를 한 번만 그리도록 정리했다.

**다음 후보**
- 요일 계획 템플릿 복사 (평일 템플릿 → 월~금 일괄 적용)
- 주간 리포트 내보내기 (HTML/PDF)
- 월간 추세 그래프 / CSV 내보내기
- 활동 그룹(공부/생활) 편집 UI, 활동 삭제
- Firestore `onSnapshot` 실시간 반영

---

## 11. 확정된 사항

1. 하루 범위 → **0시 시작 24시간** (자정을 넘겨 드래그하면 두 블록으로 자동 분할)
2. 최소 입력 단위 → **10분**
3. 로컬 데스크톱판은 폐기, **온라인 공유판만 유지**
