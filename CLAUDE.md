# 시후 하루 계획표 (Study Plan Circle)

24시간 원형 생활계획표 위에 **계획(Plan)** 과 **실행(Actual)** 을 나란히 기록하고,
그 차이를 분석해서 다음 계획을 개선하는 도구.

---

## 1. 목적

- 하루 계획을 원 그래프(24시간 시계형)로 직관적으로 입력한다.
- 같은 계획표를 오른쪽에 복사해 두고, 그 위에 **실제 실행**을 겹쳐서 기록한다.
- 계획 대비 실행의 차이(활동별 시간, 시간대별 일치율)를 수치와 그래프로 보여준다.
- 이 데이터를 근거로 "계획을 어떻게 수정할지" 가이드를 얻는다.
- 1단계는 **하루** 단위로 테스트, 검증되면 2단계로 **일주일** 단위로 확장.

---

## 2. 화면 구성

```
┌───────────────────────────────────────────────────────────────────────────┐
│  시후의 하루 계획표        [◀]  2026-08-14 (금)  [▶]        [저장] [주간]  │
├──────────────────────────────┬────────────────────────────────────────────┤
│         📘 계획 (Plan)        │          ✅ 실행 (Actual)                  │
│                              │                                            │
│         ┌──────────┐         │          ┌──────────┐                      │
│         │  24시간   │         │          │ 계획(흐림) │  ← 계획이 배경으로   │
│         │  원그래프  │         │          │ +실행(진함)│     그대로 복사됨   │
│         └──────────┘         │          └──────────┘                      │
│                              │                                            │
│  [활동 팔레트]                │  [활동 팔레트]                              │
│  ● 수학  ● 영어  ● 독서 ...   │  ● 수학  ● 영어  ● 독서 ...                 │
│                              │  [계획 그대로 복사 ⧉]                       │
│  시간 블록 리스트 (편집/삭제)   │  시간 블록 리스트 (편집/삭제)                │
├──────────────────────────────┴────────────────────────────────────────────┤
│  📊 계획 vs 실행 분석                                                       │
│  일치율 78%  |  활동별 계획/실제 막대 비교  |  자동 코멘트(개선 가이드)        │
└───────────────────────────────────────────────────────────────────────────┘
```

- 왼쪽/오른쪽 원은 **같은 크기, 같은 좌표계**. 오른쪽 원에는 계획이 항상
  옅은 톤(투명도 20%)으로 깔려 있고, 실행 기록이 그 위에 진한 도넛 링으로 겹쳐진다.
- 화면이 좁으면(<1100px) 위/아래로 자동 스택.

---

## 3. 원 그래프 규격

| 항목 | 값 |
|---|---|
| 형태 | 24시간 시계형 원 (0시 = 12시 방향, 시계방향) |
| 1시간 | 15° |
| 최소 입력 단위 | 10분 (스냅). 설정에서 5/10/15/30분 변경 가능 |
| 계획 표현 | 꽉 찬 부채꼴(파이) |
| 실행 표현 | 바깥쪽 굵은 도넛 링 (반지름 62%~100%), 계획은 그 안쪽에 옅게 |
| 눈금 | 매시 눈금, 3시간마다 숫자(0,3,6,9,12,15,18,21) |
| 렌더링 | SVG (선명하고 클릭/드래그 좌표 계산이 정확함) |

---

## 4. 입력 방식 (직관성 최우선)

1. **원 위 드래그** — 활동 팔레트에서 활동 하나를 선택한 뒤, 원 위에서 시작 각도부터
   끝 각도까지 드래그하면 그 구간이 그 활동 색으로 칠해진다. 드래그 중에는
   커서 옆에 `07:30 → 09:00 (1시간 30분)` 툴팁이 실시간 표시된다.
2. **블록 편집** — 오른쪽 리스트에서 시작/종료 시간을 select 박스로 직접 수정, 삭제.
3. **겹침 처리** — 새 블록이 기존 블록과 겹치면 **새 블록이 이깁니다**(기존 블록이
   잘리거나 분할됨). 되돌리기(Cmd+Z) 지원.
4. **지우개** — 팔레트의 지우개를 선택하고 드래그하면 그 구간이 비워진다.
5. **실행 쪽 전용** — `계획 그대로 복사 ⧉` 버튼 한 번으로 계획을 실행 쪽에 통째로
   복사한 뒤, 달라진 부분만 고쳐 그리면 된다(가장 빠른 기록 방법).
6. **지금 기록** — 실행 쪽에 "지금까지 ○○ 함" 버튼: 마지막 기록 시각 ~ 현재 시각을
   선택한 활동으로 채운다.

---

## 5. 활동(카테고리)

기본 제공 — 이름/색/그룹 모두 편집 가능하며 `activities.json`에 저장.

| 활동 | 색 | 그룹 |
|---|---|---|
| 수학 | `#4C6FFF` | 공부 |
| 영어 | `#7C5CFF` | 공부 |
| 국어/독서 | `#2FB6A5` | 공부 |
| 학교 | `#3A8DDE` | 공부 |
| 학원/과외 | `#5C7CFA` | 공부 |
| 숙제/복습 | `#00A3A3` | 공부 |
| 운동 | `#F2994A` | 활동 |
| 식사 | `#F2C14E` | 생활 |
| 휴식/자유 | `#9AA5B1` | 생활 |
| 놀이/미디어 | `#EB5757` | 생활 |
| 이동 | `#C0C7D0` | 생활 |
| 수면 | `#2E3A4B` | 생활 |

---

## 6. 분석 (하단 패널)

- **시간 일치율(%)** — 10분 슬롯 144칸 기준, 계획 활동 == 실행 활동인 슬롯 비율.
- **공부 시간 총합** — 계획 vs 실제 (예: `계획 5h 20m → 실제 4h 10m, -1h 10m`).
- **활동별 비교 막대** — 활동마다 계획/실제 두 줄 막대와 증감(±분).
- **가장 많이 어긋난 시간대 Top 3** — 예: `16:00–17:00 계획 "수학" → 실제 "놀이"`.
- **자동 코멘트(가이드)** — 규칙 기반 문장. 예시:
  - 계획 대비 실제가 계속 짧은 활동 → "수학은 3일 연속 계획보다 30분 이상 짧습니다.
    한 블록을 40분으로 줄이고 쉬는 시간을 사이에 넣어보세요."
  - 특정 시간대 이탈 반복 → "16시대는 집중이 어려운 시간대로 보입니다. 이 시간엔
    가벼운 활동을 배치해 보세요."
  - 계획에 없던 활동이 반복 등장 → "실제로는 이 시간에 ○○를 자주 합니다.
    계획에 반영하는 편이 현실적입니다."
- 메모칸 1개(그날 한 줄 회고).

---

## 7. 데이터

```
study_plan/
├── CLAUDE.md
├── app.py                       # 실행 진입점 (로컬 서버 + 브라우저 오픈)
├── static/                      # index.html · style.css · app.js
├── tools/
│   ├── build_app.py             # 더블클릭용 .app 번들 생성/갱신
│   ├── build_dist.py            # 배포용 zip (Windows / macOS)
│   └── make_icon.py             # 앱 아이콘 생성
├── dist/                        # 배포 압축 파일
├── 시후의 하루 계획표.app         # ← 더블클릭 실행 (app.py/static 사본 포함)
└── data -> ~/Library/Application Support/시후의 하루 계획표
    ├── activities.json          # 활동 목록/색
    ├── last-run.log             # 마지막 실행 로그
    └── days/
        └── 2026-08-14.json      # 하루치 기록
```

기록은 프로젝트 폴더가 아니라 `~/Library/Application Support/시후의 하루 계획표/`
에 저장하고, 프로젝트의 `data` 는 그 폴더를 가리키는 심볼릭 링크다.
`~/Documents` 안의 서명 없는 앱은 macOS 개인정보 보호(TCC) 때문에 같은 폴더의
파일조차 읽지 못하고 허용 프롬프트도 뜨지 않기 때문이다. 앱이 자기 번들 안의
파일과 Application Support 만 쓰면 이 제약을 받지 않는다.

하루치 JSON:

```json
{
  "date": "2026-08-14",
  "plan":   [{ "start": 420, "end": 480, "activity": "수학" }],
  "actual": [{ "start": 430, "end": 470, "activity": "수학" }],
  "memo": "오후에 집중이 흐트러짐",
  "updated_at": "2026-08-14T21:10:00"
}
```

- `start`/`end`는 자정 기준 **분(minute)** 값 (0~1440).
- 자동 저장(변경 후 1초 디바운스) + 수동 저장 버튼.

---

## 7-1. 주간 요약 대시보드

헤더의 `주간 요약`(또는 `⌘W`/`Ctrl+W`)으로 열리는 전체 화면 대시보드.
주는 **월요일 시작**이며 `‹ ›` 또는 `←`/`→` 로 주 단위 이동한다.

- **KPI 4종** — 주간 공부 시간(계획 대비 증감), 평균 일치율, 기록한 날 수,
  가장 잘 지킨 요일
- **요일별 미니 원그래프 7개** — 안쪽 계획 / 바깥 실행. 오늘은 테두리 강조,
  클릭하면 그 날짜의 편집 화면으로 이동한다.
- **요일별 일치율** 막대
- **활동별 주간 합계** — 활동마다 계획/실제 두 줄 막대와 주간 증감
- **시간대별 이탈 빈도** — 24개 막대. 그 시간대가 계획과 달랐던 **날 수**
  (한 시간 6칸 중 3칸 이상 어긋나면 그 날 이탈로 센다)
- **주간 가이드** — 요일·활동·시간대·총량 규칙 기반 문장

편집 화면에 떠 있는 오늘치는 아직 저장 전일 수 있으므로, 주간 데이터를 불러온 뒤
현재 화면 상태로 바꿔 끼워서 계산한다.

## 7-2. 단축키

| 키 | 동작 |
|---|---|
| `⌘S` / `Ctrl+S` | 지금 저장 |
| `⌘Z` / `Ctrl+Z` | 되돌리기 |
| `⌘W` / `Ctrl+W` | 주간 요약 열기·닫기 |
| `←` `→` | 어제/내일 (주간 요약에서는 지난 주/다음 주) |
| `Esc` | 주간 요약 닫기 |

`e.metaKey || e.ctrlKey` 로 처리해 macOS/Windows 양쪽에서 같이 동작한다.
버튼 툴팁의 표기는 `IS_MAC` 판별로 `⌘` / `Ctrl+` 를 바꿔 보여준다.

저장은 변경 후 0.7초 자동 저장 + `저장` 버튼(대기 중인 자동 저장을 취소하고 즉시
저장). 저장되면 상태 표시가 `저장됨 07:26` 처럼 시각까지 보여준다.

## 8. 기술 스택 / 실행 방식

**Python 3 표준 라이브러리만 사용. 추가 설치 없음.**

- `app.py` — `http.server` 기반 초경량 로컬 서버(127.0.0.1:8765 전용) + JSON API.
  실행하면 기본 브라우저 창이 자동으로 열린다.
- 화면 — HTML + CSS + 순수 JS + SVG. 프레임워크·CDN 없음(오프라인 동작).

**평소 사용** — `시후의 하루 계획표.app` 더블클릭.

**개발/디버깅** — `python3 app.py` (같은 데이터를 본다)

**소스를 고친 뒤** — 앱 번들 안에 사본이 들어 있으므로 반드시 다시 빌드한다:

```bash
python3 tools/build_app.py && python3 tools/build_dist.py
```

### Windows
`dist/시후의-하루-계획표-windows.zip` 을 풀고 `시후의 하루 계획표.bat` 더블클릭.
`.bat` 은 `pyw` → `pythonw` → `python` 순으로 찾아 실행하며, 앞의 둘은 콘솔 창
없이 뜬다. 파이썬이 없으면 설치 안내를 띄우고 멈춘다.

Windows 대응에서 신경 쓴 것:
- **데이터 경로** — `%APPDATA%\시후의 하루 계획표\` (`default_data_dir()`)
- **콘솔 없는 실행** — `pythonw` 는 `sys.stdout` 이 `None` 이라 `print()` 가
  터진다. cp949 콘솔에서는 한글 출력이 `UnicodeEncodeError` 를 낸다.
  두 경우 모두 `log()` 헬퍼가 삼킨다.
- **포트 중복** — Windows 의 `SO_REUSEADDR` 은 "이미 쓰는 포트에도 붙는" 의미라
  두 인스턴스가 같은 포트를 잡을 수 있다. `Server.allow_reuse_address` 를
  Windows 에서만 끈다.
- **`.bat` 인코딩** — BOM 없는 UTF-8 + CRLF. BOM 이 있으면 `@echo off` 가 깨진다.
- **zip 파일명** — 한글 파일명에 UTF-8 플래그(0x800)가 설정되어야 탐색기에서
  깨지지 않는다. 파이썬 `zipfile` 이 자동으로 설정한다.

### 실행 동작
- 런처는 서버를 떼어내 띄우고 바로 끝난다. 런처가 계속 떠 있으면 macOS 가 앱을
  '실행 중'으로 보고 다시 더블클릭해도 창이 열리지 않는다(`-600` 오류).
- 이미 서버가 떠 있으면 두 번째 실행은 서버를 새로 띄우지 않고 브라우저만 다시 연다.
- **종료는 화면 오른쪽 위 `⏻ 종료` 버튼.** 저장한 뒤 서버를 내린다. (셸 스크립트
  번들이라 Dock/⌘Q 로는 종료되지 않는다.)
- 앱 번들은 애드혹 서명(`codesign -s -`)한다. 서명이 없으면 "손상되었다"는
  경고가 뜰 수 있다.

> 선택: `pywebview`가 설치되어 있으면 브라우저 대신 **독립 데스크톱 창**으로 뜨게
> 하고, 없으면 자동으로 브라우저 모드로 폴백한다. (설치 강제 안 함)

### 왜 이 방식인가
Tkinter로도 만들 수 있지만 원 그래프의 드래그 입력, 부드러운 색/그림자, 반응형
레이아웃 품질이 크게 떨어진다. 위 방식은 설치가 필요 없으면서도 가장 깔끔한
디자인과 가장 정확한 드래그 조작을 낼 수 있다.

---

## 9. 디자인 방향

- 톤: 밝고 차분한 화이트 베이스 + 부드러운 그림자, 라운드 16px 카드.
- 폰트: 시스템 폰트 스택(Pretendard 있으면 사용, 없으면 -apple-system).
- 색: 활동 색은 채도를 한 단계 낮춘 파스텔-딥 중간 톤으로 통일해 원이 산만하지 않게.
- 다크 모드 자동 대응(`prefers-color-scheme`).
- 애니메이션은 절제 — 부채꼴 그려질 때 150ms 페이드 정도만.

---

## 10. 개발 단계

**Phase 1 — 하루 계획표 ✅ 완료**
1. ✅ 24시간 SVG 원 그래프 렌더러 (계획/실행 2개)
2. ✅ 드래그 입력 + 10분 스냅 + 겹침 처리 + 되돌리기(⌘Z)
3. ✅ 활동 팔레트 / 블록 리스트 편집 / 활동 추가
4. ✅ "계획 그대로 복사" + 실행 오버레이 + "지금까지 ○○ 함"
5. ✅ 날짜별 자동 저장/불러오기 (로컬 JSON)
6. ✅ 분석 패널 (일치율, 활동별 비교, 어긋난 시간대, 자동 가이드)
7. ✅ 더블클릭 실행용 macOS 앱 번들 + 아이콘 + UI 종료 버튼

**Phase 2 — 주간 요약 대시보드 ✅ 완료**
8. ✅ 주간 보기 (월~일 미니 원 7개, 클릭하면 그 날로 이동)
9. ✅ 주간 집계: KPI 4종, 요일별 일치율, 활동별 주간 총합, 시간대별 이탈 빈도
10. ✅ 주간 가이드 (요일/활동/시간대/총량 규칙 기반)
11. ✅ 명시적 `저장` 버튼 + 단축키
12. ✅ Windows 지원 및 배포 압축

**Phase 3 — 온라인 공유판 ✅ 완료**
13. ✅ `docs/` — 가족이 함께 보는 웹 버전 (GitHub Pages 배포)
14. ✅ Firestore 기반 데이터 계층 (로컬 JSON API를 완전히 대체)
15. ✅ 무작위 가족 코드 + 링크 공유 (로그인 화면 없음, 익명 인증)
16. ✅ 온보딩 화면(첫 방문) / 공유 팝오버(코드 확인·전환)

**Phase 4 — 다음 후보**
- 요일 계획 템플릿 복사 (평일 템플릿 → 월~금 일괄 적용)
- 주간 리포트 내보내기 (HTML/PDF)
- 월간 추세 그래프 / CSV 내보내기
- 여러 아이 프로필
- Firestore `onSnapshot` 실시간 반영 (지금은 새로고침 시에만 최신 데이터를 봄)

---

## 10-1. 온라인 공유판 (`docs/`)

`study_plan/` 안의 로컬 데스크톱판(`app.py`)과는 완전히 별개의, **여러 기기가
인터넷으로 같은 기록을 보는** 버전. GitHub Pages로 배포하고 Firebase(Firestore)를
데이터 저장소로 쓴다. 자세한 설정 방법은 [`docs/README.md`](docs/README.md) 참고.

- UI(원그래프, 드래그 입력, 팔레트, 블록 리스트, 분석, 주간 요약)는
  `static/`의 로컬판과 완전히 동일한 코드를 재사용 — 데이터 계층만 다르다.
- `fetch('/api/...')` 호출을 전부 Firestore SDK 호출로 교체
  (`docs/app.js`의 `apiGetDay` / `apiSaveDay` / `apiGetRange` / `apiGetRecent` /
  `apiGetActivities` / `apiSaveActivities`).
- **가족 구분**: 로그인 없이, 무작위로 만든 긴 코드(`familyId`) 하나가 가족
  하나를 나타낸다. `?f=코드` 링크를 열면 그 가족 공간으로 들어간다.
  Firestore 문서 경로: `families/{familyId}/days/{date}`,
  `families/{familyId}/meta/activities`.
- **인증**: Firebase 익명 인증만 사용(로그인 화면 없음). Firestore 보안 규칙은
  "로그인한 사람만" 허용 — 실제 접근 통제는 무작위 familyId 가 담당한다
  (긴 비밀 링크 공유 모델, 구글 문서 "링크 아는 사람에게 공유"와 동일한 성격).
- `firebase-config.js`의 `apiKey` 등은 비밀값이 아니라 원래 공개되는 값이라
  공개 저장소에 커밋해도 안전하다 (Firebase 표준 방식).
- 실시간 자동 반영(`onSnapshot`)은 아직 없다 — 상대방의 최신 기록을 보려면
  새로고침이 필요하다. 의도적으로 범위를 좁혀 안정성을 우선했다.

## 11. 확정된 사항

1. 하루 범위 → **0시 시작 24시간** (자정을 넘겨 드래그하면 두 블록으로 자동 분할)
2. 최소 입력 단위 → **10분**
3. 실행 형태 → **브라우저 자동 오픈** (`pywebview` 설치 시 독립 창)

## 12. 사용 메모

- **실행**: `시후의 하루 계획표.app` 더블클릭. 종료는 화면 오른쪽 위 `⏻ 종료`.
  (터미널에서는 `python3 app.py`, 브라우저를 안 열려면 `--no-open`)
- **앱을 옮기지 말 것**: 데이터는 Application Support 에 있어서 앱만 다른 폴더로
  옮겨도 동작하지만, `tools/build_app.py` 는 `study_plan/` 안을 기준으로 다시
  만들므로 원래 자리에 두는 편이 낫다. Dock 에 두려면 **별칭(alias)** 을 만든다.
- **종료 시각 24:00** 은 시간 입력칸에서 `00:00` 으로 표시된다. 종료칸의 `00:00` 은
  자정(24:00)으로 해석한다.
- 원 안쪽의 <b>공부 시간</b>은 `activities.json` 에서 `group: "공부"` 인 활동의 합.
  활동을 새로 추가하면 기본 그룹이 `공부` 이므로, 생활 활동이면
  `data/activities.json` 에서 `group` 을 `생활`/`활동` 으로 바꾼다.
- 실행 원에서 **안쪽 옅은 띠 = 계획**, **바깥 진한 띠 = 실행**. 오늘 날짜면
  현재 시각이 빨간 바늘로 표시된다.
- 모든 데이터는 `data/` 안에만 있다. 폴더째 백업/이동하면 그대로 옮겨진다.
